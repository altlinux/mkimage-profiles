=== features.in/alt-components === 

Фича предназначена для работы с альтератор компоненты (alterator-comonents)
и редакциями (editions) на их основе.
На текущий момент предоставляется возможность добавлять в iso-образ пакеты,
которые входят в ту или иную редакцию через переменную EDITION_PACKAGES.
