label vncpasswd
  menu label VNC install (<Tab>, set pass^word and connect here)
  kernel /boot/vmlinuz
  append initrd=/boot/@initrd@ fastboot @initrd_bootargs@ @stagename@=live @install2_init@ ramdisk_size=@live_size@ showopts nosplash lowmem @bootargs@ headless no_alt_virt_keyboard vncpassword=VNCPWD

